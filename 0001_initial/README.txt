All this version can do is generate a program that will compile a program that
will emit a single number.

./mk
./test.sh

Reference main.c is from chibicc. main.js is the js port. mk will convert the js
version to C and then compile and run it via cc_x86
